[project]
name = "nexa"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111",
  "uvicorn[standard]>=0.29",
  "websockets>=12",
]

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q"
