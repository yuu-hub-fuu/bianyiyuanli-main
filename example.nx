fn main() -> i32 {
  os.setenv("NEXA_DEMO", "hello");

  let value: str = os.getenv("NEXA_DEMO");
  let cwd: str = os.getcwd();

  os.sleep(100);

  if str.len(value) > 0 && str.len(cwd) > 0 {
    return str.len(value);
  }

  return 0;
}
